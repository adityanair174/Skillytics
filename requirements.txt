# skillytics requirements
# Install:
#   python -m venv .vision
#   source .vision/bin/activate
#   python -m pip install -U pip setuptools wheel
#   python -m pip install -r requirements.txt

# ============================================================================
# CORE DEPENDENCIES (Required for stitching)
# ============================================================================
# Computer Vision / Image Processing
opencv-python==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# ============================================================================
# OPTIONAL: MediaPipe (for pose estimation / activity tracking)
# ============================================================================
# Uncomment if using pipelines/activity/ scripts
# mediapipe==0.10.31
# protobuf>=3.20.3,<5

# ============================================================================
# OPTIONAL: YOLOX / ByteTrack (for object tracking)
# ============================================================================
# Uncomment if using pipelines/tracking/ scripts
# NOTE: Do NOT pip-install ByteTrack as a package (it contains a `yolox/` too and can break imports).
# Use ByteTrack via PYTHONPATH only when running tracking scripts.
# After installing, also run:
#   python -m pip install -e third_party/YOLOX

# --- Core ML (for YOLOX) ---
# torch==2.2.2
# torchvision==0.17.2

# --- YOLOX common deps ---
# loguru==0.7.2
# tqdm==4.66.5
# tabulate==0.9.0
# thop==0.1.1.post2209072238
# scipy==1.11.4
# matplotlib==3.8.4

# --- COCO tools (for YOLOX) ---
# pycocotools==2.0.7

# --- Optional export tools (for YOLOX) ---
# onnx==1.16.1
# onnxruntime==1.18.1
# tensorboard==2.17.0
